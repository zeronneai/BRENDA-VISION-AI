# Copy this file to .env.production.local and fill in your values.
# NEVER commit .env.production.local — it's blocked by .gitignore.

# Full URL of your Vercel deployment (no trailing slash).
# Used by the native iOS app to reach the API.
VITE_API_URL=https://YOUR_VERCEL_URL_HERE.vercel.app
